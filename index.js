
let userPreference = 'Sugar-free'; 
console.log('this user prefers', userPreference ? userPreference : 'Regular');
//
let cansLeft = 3; 
console.log(cansLeft < 5 ? 'Time to stock':'We re stocked'   );//check if cans left is less than 5 or more...then stock more cans


let heartRate = 95; // Current heart rate in bpm
console.log(heartRate < 100 ? 'Boost needed': 'Energy levels are high');//check if bpm is less than 100 then need to boost.


let currentTemp = 4; // Current temperature in °C
console.log(currentTemp <= 5 ? 'Chilled to perfection!' : 'Needs a cooler!');



let currentHour = 22;


console.log(currentHour >= 7 && currentHour < 24 ? 'unleash the beast': 'better stick to water');
//using logical AND operate to compare the current hour on two 
//conditions at the same time



